# CLAUDE.md

Repo-specific guidance for `model-health-docs`. Shared platform context is auto-loaded
from the parent folder; generic coding behavior from `~/.claude/CLAUDE.md`.

## Where this repo sits

Documentation for the platform and SDK. Content here should stay consistent with the
behavior of `../api` and `../model-health-internal`.

## Stack

- <!-- TODO: docs framework (e.g. Docusaurus / MkDocs / Mintlify), build + preview commands -->

## Build / preview

<!-- TODO -->

## Gotchas

- Docs describing API or SDK behavior must match the **current** implementation —
  verify against `../api` (V2 endpoints) and `../model-health-internal` before
  documenting.
- Sentence case, concise. <!-- TODO: house style, if any -->
