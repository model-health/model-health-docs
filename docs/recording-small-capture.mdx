---
id: recording-small-capture
title: Small capture areas
sidebar_label: Small capture areas
---


Best practices for recording activities that require a small capture area, such as countermovement or drop vertical jumps.

:::tip
Before finalizing your setup, have the participant perform the activity while you monitor each camera's field of view, and adjust camera orientation to ensure the full movement is visible from all cameras.
:::

## 1. Recording Setup

Ensure cameras are correctly positioned to capture the full movement within the capture area.

### Set up the cameras

- Mount the iOS devices on tripods in **portrait mode**.
- Use the setup diagram below as a guide for tripod placement and angles.
- Open the Model Health app on each device and adjust positions until the **full capture area is visible** on every screen.

<div style={{textAlign: 'center', margin: '1.5rem 0'}}>
  <img src="/img/docs/recording-small-capture/setup-small-capture.png" alt="Large capture area setup diagram" style={{width: '50%'}} />
  <p style={{fontSize: '0.8rem', color: 'var(--ifm-color-secondary-darkest)', marginTop: '0.4rem'}}>
    Two sagittal (~30-40°) cameras (portrait) positioned ~2 m from the capture area, with an optional front-facing camera (portrait).
  </p>
</div>

### Verify the field of view

:::tip
Use cones to mark the capture area boundaries and adjust the camera positions until all cones are visible in each view.
:::

<div style={{display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '1rem', margin: '1.5rem 0'}}>
  <div style={{textAlign: 'center'}}>
    <img src="/img/docs/recording-small-capture/small-volume-capture-left-camera-portrait.png" alt="Left camera field of view" style={{width: '100%', borderRadius: '6px'}} />
    <p style={{fontSize: '0.8rem', color: 'var(--ifm-color-secondary-darkest)', marginTop: '0.4rem'}}>Left camera — verify field of view</p>
  </div>
  <div style={{textAlign: 'center'}}>
    <img src="/img/docs/recording-small-capture/small-volume-capture-right-camera-portrait.png" alt="Right camera field of view" style={{width: '100%', borderRadius: '6px'}} />
    <p style={{fontSize: '0.8rem', color: 'var(--ifm-color-secondary-darkest)', marginTop: '0.4rem'}}>Right camera — verify field of view</p>
  </div>
</div>

## 2. Setup calibration

- Place the checkerboard **perpendicular to the ground**, with its long edge parallel to the ground, visible to all devices, approximately **3-5 meters from each device**.
- Once ready, press **"Calibrate"**.

Once the calibration is complete, the checkerboard can be removed.

## 3. Static pose

- Have the participant stand in a static pose near the checkerboard position: upright, feet parallel and pointing forward, arms slightly away from the body.