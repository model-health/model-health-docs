---
id: advanced-settings
title: Advanced settings
sidebar_label: Advanced settings
---

export const ZoomableImage = ({ src, alt, caption }) => {
  const [open, setOpen] = React.useState(false);
  return (
    <>
      <div style={{textAlign: 'center'}}>
        <img
          src={src} alt={alt}
          onClick={() => setOpen(true)}
          style={{width: '80%', cursor: 'zoom-in', borderRadius: '6px'}}
        />
        {caption && <p style={{fontSize: '0.8rem', color: 'var(--ifm-color-secondary-darkest)', marginTop: '0.4rem'}}>{caption}</p>}
      </div>
      {open && (
        <div onClick={() => setOpen(false)} style={{position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.85)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 9999, cursor: 'zoom-out', padding: '2rem'}}>
          <img src={src} alt={alt} style={{maxWidth: '90vw', maxHeight: '90vh', borderRadius: '8px', boxShadow: '0 8px 40px rgba(0,0,0,0.6)'}} />
        </div>
      )}
    </>
  );
};

import React from 'react';

:::info
Advanced settings let you customize how Model Health calibrates your setup, records, and analyzes your data. Apart from checkerboard settings, we recommend keeping the defaults unchanged — they have been carefully selected to provide the best results.
:::

Settings are spread across three panels that appear at different points in the recording flow.

---

## 1. Checkerboard settings
**When:** During setup, at the *Position & Calibrate* step.

These settings let you configure the physical checkerboard used to calibrate your cameras. Open the **Checkerboard settings** panel on the right side of the calibration screen.

<ZoomableImage
  src="/img/docs/advanced-settings/setup-calib-settings.png"
  alt="Checkerboard settings panel"
  caption="Checkerboard settings appear during the setup calibration step."
/>

### Board position
Choose how the checkerboard will be placed during calibration:
- **Perpendicular to the floor** *(default)* — board stands upright, perpendicular to the ground.
- **Lying on the floor** — board lies flat on the ground.

### Rows and Columns
Enter the number of **inner corner rows** and **inner corner columns** on your printed board. Default is **4 rows × 5 columns**. Match these exactly to your printed checkerboard.

### Square size (mm)
Enter the size of one square on your checkerboard in millimetres. Default is **35 mm**. Measure your printed board and enter the accurate value — an incorrect size will affect the scale of the reconstruction.

:::warning Adjusting the default
Adjusting checkerboard parameters on this panel only applies to the current calibration step.
If you want to adjust the default values for all future setups, you can **update your checkerboard dimensions in the App Settings** — this is critical for accurate measurements.
<ZoomableImage src="/img/docs/getting-started/update-checkerboard-default.png" alt="Checkerboard setup" style={{width: '280px', marginLeft: '2rem', marginBottom: '0.5rem'}} />
 
:::

---

## 2. Subject scaling settings
**When:** During the recording flow, at the *Record static pose* step.

You can adjust the model scaling settings before recording the neutral pose. Open the **Subject scaling settings** panel — the button appears below the example neutral pose image on screen.

<ZoomableImage
  src="/img/docs/advanced-settings/model-calib-settings.png"
  alt="Subject scaling settings panel"
  caption="Subject scaling settings appear during the static pose recording step."
/>

### Scaling setup
We use the neutral pose to scale the musculoskeletal model to the subject's body dimensions.

- **Upright standing pose** *(recommended, default)* — assumes the subject stands upright with feet pointing forward, straight back, no bending or rotation at hips, knees, or ankles.
- **Any pose** — use this if the subject cannot adopt an upright neutral pose. All body segments must be visible to at least two cameras, and the subject must remain still during the recording.

### Musculoskeletal model
Choose the biomechanical model to use for this analysis:

- **Full body model with ISB shoulder** *(default)* — a 33 degree-of-freedom model based on [Lai et al. 2017](https://pubmed.ncbi.nlm.nih.gov/28900782/) with modified hip abductor paths from [Uhlrich et al. 2022](https://pubmed.ncbi.nlm.nih.gov/35798755/), plus a 6 degree-of-freedom shoulder complex using the ISB-recommended Y-X-Y glenohumeral rotation sequence ([Wu et al. 2005](https://pubmed.ncbi.nlm.nih.gov/15844264/)). Recommended for upper extremity tasks and suitable for all activities.
- **Full body model** — the same model with a simpler 3 degree-of-freedom shoulder complex.

### Core engine
The core engine converts raw videos into 3D motion data.

- **v1.0** *(recommended, default)* — latest version, more accurate and robust across activities. Recommended.
- **v0.3** *(old model, default until 04-01-2026)* — previous version (formerly named marker augmenter v0.3).
- **v0.2** *(old model, default until 07-30-2023)* — legacy version (formerly named marker augmenter v0.2).

---

## 3. Video Recording Settings
**When:** During the recording flow, at the *Activity recording* step.

You can adjust recording parameters before starting a recording. Click the **Recording settings** button at the bottom-left of the activity recording screen.

<ZoomableImage
  src="/img/docs/advanced-settings/video-recording-settings.png"
  alt="Video recording settings panel"
  caption="Video recording settings appear on the activity recording screen, before starting a recording."
/>

### Frame rate
Record videos at **60, 120, or 240 fps**. The default varies by activity type.

- Higher frame rates capture fast movements more accurately but increase upload and processing time.
- Defaults: **120 fps** for most activities, **60 fps** for walking, squats, and sit-to-stand, and **240 fps** for sprinting.
- Use **60 fps** if processing speed is a priority — consider applying a filter (see below) to reduce noise.

### Filter frequency
We apply a low-pass Butterworth filter to smooth 2D video keypoints before reconstruction.

- Defaults: **20 Hz**, except for walking, squats, and sit-to-stand activities (**12 Hz**).

Enter a custom value in Hz, or leave it as `default`.

:::warning
- Per the Nyquist theorem, the filter frequency must be less than half the frame rate. Values above this limit will be capped automatically.
- Consult the literature for a suitable frequency for your specific task. When in doubt, keep the default.
:::
